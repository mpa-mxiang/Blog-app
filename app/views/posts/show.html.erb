<!DOCTYPE html>
<html>
<head>
</head>
<body>
<div class="border border-dark w-75 m-3">
  <h2><%= @post.title %> by <%= @post.author.name %></h2>
  <span>Comments: <%= @post.comments_counter %></span>
  <span>Likes: <%= @post.likes_counter %></span>
  <p><%= @post.text %></p>
  <% @post.recent_comments.each do |comment| %>
    <li class="border border-dark w-75 m-3 list-unstyled"><%= render 'comment', comment: comment %></li>
  <% end %>
  </div>
  <div class="btn-div">
  <%= link_to "Home", users_path %>
</div>
</body>
</html>