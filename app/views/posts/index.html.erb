<!DOCTYPE html>
<html>
<head>
  <title>All Posts</title>
</head>
<body>
  <h1>All Posts</h1>
  <ul>
    <% @posts.each do |post| %>
      <li>
        <h2><%= post.title %></h2>
        <p><%= post.created_at.strftime('%B %d, %Y') %></p>
        <p><%= post.text %></p>
      </li>
    <% end %>
  </ul>
</body>
</html>
