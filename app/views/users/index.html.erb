<!DOCTYPE html>
<html>
<head>
  <title>All Users</title>
  <link rel="stylesheet" type="text/css" href="/assets/stylesheets/users.css">
</head>
<body>
  <h1>All Users</h1>
  <ul class="user-list">
    <% @users.each do |user| %>
      <li class="user-item">
        <img src="<%= user.photo %>" alt="<%= user.name %>">
        <h3><%= user.name %></h3>
        <p class="user-info">Number of Posts: <%= user.posts.count %></p>
      </li>
    <% end %>
  </ul>
</body>
</html>