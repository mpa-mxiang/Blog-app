<!DOCTYPE html>
<html>
<head>
  <title>All Users</title>
</head>
<body>
  <h1>All Users</h1>
  <ul>
    <% @users.each do |user| %>
      <li>
        <img src="<%= user.photo %>" alt="<%= user.name %>">
        <h1><%= user.name %></h1>
        <p>Number of Posts: <%= user.posts.count %></p>
      </li>
    <% end %>
  </ul>
</body>
</html>
